import { CardDirectiveDirective } from './card-directive.directive';

describe('CardDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CardDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
